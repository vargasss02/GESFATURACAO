import { Provider as PaperProvider } from 'react-native-paper';

export default function App() {
  return (
    <PaperProvider>
      {/* Aqui vais chamar o teu componente de navegação principal */}
    </PaperProvider>
  );
}